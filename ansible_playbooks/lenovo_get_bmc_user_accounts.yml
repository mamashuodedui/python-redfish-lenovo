- hosts: myhosts
  name: get bmc user accounts
  gather_facts: False

  tasks:
  - name: Define output file
    include_tasks: create_output_file.yml type=GetBmcUserAccounts

  - name: run getter scripts based on script_name
    local_action: shell python ../examples/lenovo_get_bmc_user_accounts.py {{baseuri}} {{user}} {{password}}
    register: script_output

  - name: Copy results to output file
    local_action: copy content={{ script_output.stdout_lines | to_nice_json }}
       dest={{template}}.txt

  - debug: var=script_output.stdout

